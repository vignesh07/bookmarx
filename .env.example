# Postgres connection string. Locally: `postgres://postgres:postgres@localhost:5432/bookmarx`.
# When hosted: copy from whatever Postgres service you provisioned.
DATABASE_URL=

# Shared secret. The browser extension sends this in `X-Bookmarx-Token` to authorize ingest.
# Generate one with: `openssl rand -hex 32`
INGEST_TOKEN=
